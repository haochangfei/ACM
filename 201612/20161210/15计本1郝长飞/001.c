/*
1、题目描述：求两个有序数组的公共元素
给定两个含有n个元素的有序（非降序）整型数组a和b，求出其共同元素，比如:
a = 0, 2, 4, 5, 10
b = 2, 3, 5, 7, 9
输出 1, 5
题目要求：使用尽可能高效的算法，并分析程序的时间、空间复杂度
*/
#include<stdio.h>
main()
{
  int i=0,j=0,k=0;
  int a[]={0,2,4,5,10};
  int b[]={2,3,5,7,9};
  while(i<5&&j<5&&k++<5)
  {
      if(a[i]==b[j])
      {
        printf("%d,",a[i]);
        i++;
        j++;
      }
      else if(a[i]<b[j])
        i++;
      else if(a[i]>b[j])
        j++;
  }
}
